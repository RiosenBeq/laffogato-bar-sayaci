# Laffogato — ayarlar
# Bu dosya .env olarak kopyalanır (başlatıcı ilk açılışta otomatik yapar).

# KAYNAK — bar alanını gören kamera:
#   0            → bilgisayarın kendi kamerası (canlı deneme için en kolayı)
#   1            → USB ile takılı ikinci kamera
#   rtsp://...   → IP kamera (kafedeki güvenlik kamerası)
#   veri/ornek.mp4 → kayıtlı video dosyası
KAYNAK=0

# Saniyede kaç kare analiz edilsin (bar alanı için 3-5 yeterli)
KARE_FPS=4

# Tespit modeli
MODEL_DOSYASI=models/yolox_tiny.onnx

# cpu | cuda
CIHAZ=cpu
